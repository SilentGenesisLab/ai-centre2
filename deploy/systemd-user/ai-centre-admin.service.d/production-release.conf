[Service]
# Durable guard against legacy deployment scripts that overwrite the main unit
# and point it back at admin/.next while `next build` is replacing that tree.
WorkingDirectory=/home/donxu/ai-centre/runtime/admin/current
ExecStart=
ExecStart=/home/donxu/.local/node/bin/node /home/donxu/ai-centre/runtime/admin/current/server.js
