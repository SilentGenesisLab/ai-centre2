# AI Centre 水印处理（合规运行验证）生产测试报告

- 测试批次：`watermark-validation-20260818`
- 开始时间（UTC）：`2026-08-18T04:09:16.542738+00:00`
- 完成时间（UTC）：`2026-08-18T04:09:20.197323+00:00`

## 一、执行摘要

| 指标 | 结果 |
| --- | ---: |
| 业务请求 | 1 |
| 提交成功率 | 0.00% |
| 最终完成率 | 0.00% |
| P50 / P95 / P99总耗时 | — / — / — 秒 |
| 安全拒绝通过率 | 100.00% |
| 取消接口 | 不适用（任务未进入队列） |

## 二、并发性能

| 并发 | 请求数 | 完成率 | P50 | P95 | P99 |
| ---: | ---: | ---: | ---: | ---: | ---: |
| — | — | — | — | — | — |

## 三、合规边界与服务状态

- Worker是否运行：`False`。
- 本报告只验证自有合成素材的接口、任务状态、结果容器和画质损失，不测试或宣称能够规避任何平台的隐藏水印、溯源标识或风控检测。
- 生产服务器没有安装 `ai-centre-watermark-worker.service`。
- 异步提交返回HTTP 503，直接原因是服务器上的`control_plane/config.py`缺少`watermark_result_expires_seconds`，任务客户端抛出`AttributeError`，因此任务尚未写入队列。
- Worker和配置均缺失时不会调用长等待接口，也没有产生需要取消的僵尸任务。

## 四、异常与结论

- 未完成、HTTP异常、安全拒绝或取消不符合预期的记录：1 条。
- 当前结论是“模块代码存在、生产部署不完整”，不是处理效果不达标；在补齐配置字段和专用Worker之前无法开展light/intensive结果质量测试。
- 逐任务提交、轮询次数、阶段、OSS结果、媒体探测和错误详情保存在 `results.csv/json`。
- 资源数据每秒采样；本次不执行GPU启停，也不修改生产服务配置。

## 五、产物

- `PROCESS.zh-CN.md`：可复现测试流程。
- `results.csv/json`、`summary.json`：逐任务及聚合结果。
- `result-samples/`：首个结果视频和关键帧样本。
- `resources.csv/json`：GPU、CPU、内存、功耗和温度采样。
